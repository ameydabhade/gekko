<div class="julysale-banner">
   <img class="desktop" src="{{ 'aug-sale-banner.png' | asset_url }}" alt="Gift Card">
   <img class="mobile" src="{{ 'aug-sale-bannermobile.png' | asset_url }}" alt="Gift Card">
    <div class="container-fluid">
      <div class="julysale-banner-overlay">
        <div class="countdown-inner">  
          <div class="offer-note">
            <p>Free Gift With Purchases <i><b>Sale Ends in</b></i></p>
          </div> 
          <div id="countdowns"></div>
        </div> 
        <!-- <span>Celebrate the 4th of JulY</span> -->
        <!-- <h1>Buy Any 3 Items, Get The 4th Free</h1> -->
        <!-- <p class="dis">Stock up, Spend Less, Explore More</p>
        <a href="/collections" class="btn-shop">SHOP NOW</a>
        <span class="giftcard-popup">*See More</span> -->
      </div>
    </div>
</div>
{% schema %}
{
  "name": "July Sale Banner",
  "settings": [],
   "presets": [{
  "name": "July Sale Banner"
}]
}  
{% endschema %} 

<!-- <script>  
$(document).ready(function() { 
    var targetDate = new Date('2024-07-04T23:59:00-04:00').getTime();    
     
    function startCountdown() {
        var countdownInterval = setInterval(function() {
            var currentDate = new Date().getTime();
            var timeRemaining = targetDate - currentDate;

            if (timeRemaining >= 0) {
                var days = Math.floor(timeRemaining / (1000 * 60 * 60 * 24));
                var hours = Math.floor((timeRemaining % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
                var minutes = Math.floor((timeRemaining % (1000 * 60 * 60)) / (1000 * 60));
                var seconds = Math.floor((timeRemaining % (1000 * 60)) / 1000);

                // Add leading zero if the value is less than 10
                days = days < 10 ? '0' + days : days;
                hours = hours < 10 ? '0' + hours : hours;
                minutes = minutes < 10 ? '0' + minutes : minutes;
                seconds = seconds < 10 ? '0' + seconds : seconds;

                function createDigitDivs(number) {
                    return String(number).split('').map(digit => `<div class='digit'>${digit}</div>`).join(''); 
                } 

                $('#countdowns').html(
                    "<div class='box days'>" + createDigitDivs(days) + "</div>" +
                    "<div class='space'>:</div>" +
                    "<div class='box hours'>" + createDigitDivs(hours) + "</div>" +
                    "<div class='space'>:</div>" +
                    "<div class='box minutes'>" + createDigitDivs(minutes) + "</div>" +
                    "<div class='space'>:</div>" +
                    "<div class='box seconds'>" + createDigitDivs(seconds) + "</div>"
                );
            } else {
                clearInterval(countdownInterval);  
                $('.countdown-inner').hide();
            }
        }, 1000); 
    }

    startCountdown();
});
</script> -->